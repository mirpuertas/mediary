name: med_journal
description: "Mediary — local-first medication and sleep journal with reminders."
publish_to: 'none'

version: 1.0.0+2

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # State management
  provider: ^6.1.0

  # Database & Storage
  sqflite: ^2.3.0
  path: ^1.9.0
  shared_preferences: ^2.2.0
  path_provider: ^2.1.0

  # UI & Utilities
  intl: ^0.19.0
  collection: ^1.18.0
  table_calendar: ^3.1.0
  timezone: ^0.9.0
  flutter_timezone: ^3.0.1

  # Notificaciones
  flutter_local_notifications: ^18.0.1
  permission_handler: ^11.3.1
  device_info_plus: ^10.1.0

  # Sharing & Export
  share_plus: ^7.2.0
  csv: ^6.0.0
  pdf: ^3.11.1
  printing: ^5.13.3

  cupertino_icons: ^1.0.8
  excel: 4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon/icon.png"

flutter:
  uses-material-design: true

  assets:
    - assets/icon/icon.png
